

1. provided a bounding box where all the nodes and edges have been taken; because i cant understand which ndoes and edges are being taken and therefore i might click to nodes which havent been taken
2. once the start and finish point is fixed, keep it permanant unless we change it via the left sidebar
button, change
3. Remove the simple mode; all should be advanced mode
4. Remove the Route Profile
4.1.  Remove the DATE AND TIME option  
5. The manual Road Overdrive should like this, after calculating the path for point A to B, i will select one path; and increase its traffic and then i will click re-calculate, it will then suggest an alternate route
6. Add weather based heuristics, for example: 
    1. Blizzard : random one of the routes will be blocked
    2. Rainy : any random two roads will have severe traffic
    3. storm : any one road will be blocked

